name: amqp
workers: 1

input:
  use: amqp
  codec:
    use: json
    options:
  options:
    username: calameo
    password: password
    vhost: /calameo
    exchange_name: platform.events
    queue_name: shovel
    routing_key: '#'

pipeline:
  options:

output:
  use: amqp
  codec:
    use: json
    options:
  options:
    username: calameo
    password: password
    vhost: /calameo
    exchange_name: platform.events.retry
    routing_key: 'test'

# output:
#   use: stdout
#   codec:
#     use: json
#     options:
#       pretty: true