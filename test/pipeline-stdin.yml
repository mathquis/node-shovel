name: stdin
workers: 1

input:
  use: stdin

decoder:
  split: true
  use: json
  options:

pipeline:
  options:

encoder:
  use: json
  options:
    pretty: true

output:
  use: stdout
  options:
