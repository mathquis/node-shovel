name: stdin
workers: 1

input:
  use: stdin
  split: true
  codec:
    use: json
  options:

pipeline:
  options:

output:
  use: stdout
  codec:
    use: json
    options:
      pretty: true
  options:
